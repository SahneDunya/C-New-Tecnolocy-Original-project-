#include "cnt_spi.h"

// SPI Başlatma Fonksiyonu
bool SPI_Init(uint32_t port, SPI_InitTypeDef* init) {
    // Donanım bağımlı SPI başlatma mantığı
    // ... SPI başlatma mantığı ...

    // Başarılı ise true döndür
    return true;
}

// SPI Veri Gönderme Fonksiyonu
bool SPI_Transmit(uint32_t port, uint8_t* data, uint32_t size) {
    // Donanım bağımlı SPI veri gönderme mantığı
    // ... SPI veri gönderme mantığı ...

    // Başarılı ise true döndür
    return true;
}

// SPI Veri Alma Fonksiyonu
bool SPI_Receive(uint32_t port, uint8_t* data, uint32_t size) {
    // Donanım bağımlı SPI veri alma mantığı
    // ... SPI veri alma mantığı ...

    // Başarılı ise true döndür
    return true;
}