#include "cnt_uart.h"

// UART Başlatma Fonksiyonu
bool UART_Init(uint32_t port, UART_InitTypeDef* init) {
    // Donanım bağımlı UART başlatma mantığı
    // ... UART başlatma mantığı ...

    // Başarılı ise true döndür
    return true;
}

// UART Veri Gönderme Fonksiyonu
bool UART_Transmit(uint32_t port, uint8_t* data, uint32_t size) {
    // Donanım bağımlı UART veri gönderme mantığı
    // ... UART veri gönderme mantığı ...

    // Başarılı ise true döndür
    return true;
}

// UART Veri Alma Fonksiyonu
bool UART_Receive(uint32_t port, uint8_t* data, uint32_t size) {
    // Donanım bağımlı UART veri alma mantığı
    // ... UART veri alma mantığı ...

    // Başarılı ise true döndür
    return true;
}